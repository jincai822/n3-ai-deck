# N3 V3.0 (6602:1000) v0.1.0 Release Gate

| Field | Result |
|---|---|
| Date | 2026-08-05 |
| Tested commit | `8a8ca9e` |
| Gate | v0.1.0 release preconditions short of the final push and tag (owner-gated publish actions) |
| Local gates | `uv run pytest -q` → 951 passed; `uv run ruff check .` → all checks passed; `uv build` → `dist/streamdock_n3_linux-0.1.0.tar.gz` + `dist/streamdock_n3_linux-0.1.0-py3-none-any.whl` |
| Physical validation checklist | 10/10 rows Pass (`docs/validation/physical-validation-checklist.md`) |
| Artifacts | wheel `streamdock_n3_linux-0.1.0-py3-none-any.whl`; sdist `streamdock_n3_linux-0.1.0.tar.gz` |

## 1. Local gates

`uv run pytest -q`:

```
951 passed in 7.39s
```

`uv run ruff check .`:

```
All checks passed!
```

`uv build`:

```
Building wheel from source distribution...
Successfully built dist/streamdock_n3_linux-0.1.0.tar.gz
Successfully built dist/streamdock_n3_linux-0.1.0-py3-none-any.whl
```

`dist/` contains exactly the two `0.1.0` artifacts; the inherited `0.2.5`
artifacts were removed before the build.

Wheel verification: METADATA reports `Version: 0.1.0`; the wheel contains
`streamdock_n3/resources/actions.default.json` and
`streamdock_n3/resources/keymaps/6602-1000.json`. The sdist contains
`CHANGELOG.md`, `docs/ARCHITECTURE.md`, and
`docs/validation/physical-validation-checklist.md`.

Artifact digests (SHA256SUMS manifest):

```
6bb167cdec956332ec0278027a6c0d4396acad2a8f39d414419c861ace888d7a  streamdock_n3_linux-0.1.0-py3-none-any.whl
93425793d5b71a3708af68da4f49fa792d37c9db0e4cedb8f40e0df63dced4fe  streamdock_n3_linux-0.1.0.tar.gz
```

## 2. Physical validation checklist

The M5 precondition "the physical validation checklist pass" is met: all 10
rows of `docs/validation/physical-validation-checklist.md` are Pass — M1
passive discovery; G1 candidate profile approval; G2 offline permission plan;
G3 evdev falsification and vendor-channel input for all 12 controls; G4–G6
display (init, brightness, single key image); G7 six LCDs plus production
input regression; M3 action-engine smoke; M3 live dispatch 23/23; M4 AI
workflow golden 11/11 — each backed by its dated evidence record.

## 3. CI status and gate procedure

The fork CI (`.github/workflows/ci.yml`: ruff, advisory mypy, pytest, and a
build smoke step on Python 3.11 and 3.12, on every push to `main` and pull
request) last ran green on the pushed `main` at commit `a3874de`; that run
concluded success on both matrix legs. The three newest commits (`93cb53d`,
`053e7b2`, `8a8ca9e`) are not yet pushed.

Gate procedure (explicit ordering):

1. Push `main` to the owner's GitHub fork.
2. CI must go green on the release commit (ruff + advisory mypy + pytest +
   build on both Python versions).
3. Tag `v0.1.0` on the green release commit.
4. Create the GitHub Release for `v0.1.0` and attach the wheel, the sdist,
   and the SHA256SUMS manifest.

The local-gate results in section 1 are the current evidence for this
procedure.

## 4. Open items not blocking v0.1.0

- G8 daemon-managed background wiring (auto-restart, auto-reconnect,
  background live dispatch) — not included in v0.1.0.
- Vendored-SDK commercial redistribution review — still open per the PRD;
  v0.1.0 is an open-source release consistent with the upstream project's own
  distribution.
- PyPI publishing — not done; v0.1.0 ships as a GitHub Release only.
- Per-key LCD anchor offset calibration (individual 1–2 px deviations) —
  cosmetic follow-up.

## Conclusions

All release preconditions short of the final push and tag are met: local
gates are green (951 tests passed, ruff clean, reproducible `0.1.0` build
with only the two expected artifacts in `dist/`), the physical validation
checklist passes 10/10, and the fork CI is green on the last pushed `main`
commit. The remaining steps are the owner-gated publish actions: push
`main`, confirm CI on the release commit, tag `v0.1.0`, and create the
GitHub Release.

## Constraints honored / redaction

- The release artifacts are reproducible local builds; no publishing action
  was taken by this record.
- No serial, bus location, `/dev` node name, absolute path, or username is
  recorded here; the fork is referred to only as the owner's GitHub fork.
