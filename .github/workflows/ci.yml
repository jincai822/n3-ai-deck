name: ci

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

# Force Node 24 for actions that haven't migrated yet (e.g. setup-uv@v6).
# Drop this once all referenced actions ship a Node-24 runtime by default.
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  lint-test-build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v5

      - name: Install system libs for evdev / Pillow
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libevdev-dev libudev-dev libjpeg-dev zlib1g-dev

      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Install dev dependencies
        run: uv sync --extra dev

      - name: Ruff
        run: uv run ruff check .

      - name: Mypy
        run: uv run mypy src/streamdock_n3 || true   # advisory until baseline is clean

      - name: Pytest
        run: uv run pytest

      - name: Build wheel + sdist (smoke)
        run: uv build
